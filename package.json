{
  "name": "agent-worker",
  "version": "1.0.0",
  "private": true,
  "description": "HOOX AI risk manager worker: configurable cron (1\u20131440 min), trailing stops, multi-provider LLM fallback (Workers AI \u2192 OpenAI \u2192 Anthropic \u2192 Gemini), kill switch.",
  "license": "Apache-2.0",
  "type": "module",
  "dependencies": {
    "@hoox-sh/hoox-shared": "workspace:*"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  },
  "author": "jango-blockchained (hoox-sh)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hoox-sh/agent-worker.git"
  }
}
